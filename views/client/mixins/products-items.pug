mixin products-items(item)
  .col-4.mb-3
    .product-item
        .inner-image
            img(src=item.thumbnail)
            if(item.featured == "1")
              .inner-featured Nổi bật
        .inner-content
            h3.inner-title
              a(href=`/products/${item.slug}` style="text-decoration: none;") #{item.title}
            .inner-price-new #{item.priceNew}$
            .inner-price-old #{item.price}$
            .inner-discount #{item.discountPercentage}%